/*
 * driver.cpp
 *
 *  Created on: Apr 4, 2013
 *      Author: brian
 */

#include "clientData.h"
#include "listType.h"
int main(int argc, char **argv) {

	listType mygpa;

//
//	fstream fout("mycredit.txt", ios::in | ios::out | ios::binary);
//	ClientData businessGuy;
//
//	for (int i = 0; i < 5; ++i) {
//		businessGuy.setAcctNum(i+1);
//		fout.write(reinterpret_cast<const char *>(&businessGuy), sizeof(ClientData));
//	}
//
//	for (int i = 0; i < 5; ++i) {
//	fout.seekg(i*sizeof(ClientData)); // beginning of file.
//    fout.read(reinterpret_cast<char *>(&businessGuy), sizeof(ClientData));
//    cout << businessGuy.getAcctNum()
//    		<< "\t" << businessGuy.getName()
//    		<< "\t" << businessGuy.getBalance() << endl;
//    businessGuy.setDouble(100 * businessGuy.getAcctNum()+businessGuy.getBalance());
//	fout.seekp(i*sizeof(ClientData)); // beginning of file.
//	fout.write(reinterpret_cast<const char *>(&businessGuy), sizeof(ClientData));
//	}
//	for (int i = 0; i < 5; ++i) {
//		fout.seekg(i*sizeof(ClientData)); // beginning of file.
//	    fout.read(reinterpret_cast<char *>(&businessGuy), sizeof(ClientData));
//	    cout << businessGuy.getAcctNum()
//	    		<< "\t" << businessGuy.getName()
//	    		<< "\t" << businessGuy.getBalance() << endl;
//	}

    return (0);

}



